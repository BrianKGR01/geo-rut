import type { LatLng } from "@/types/domain";

// Sin ubicación ni tiendas hay que mostrar algo: centro de Lima (ver DECISIONS.md).
export const DEFAULT_CENTER: LatLng = { lat: -12.0464, lng: -77.0428 };
